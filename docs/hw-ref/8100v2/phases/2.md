# Capture 2 — phase timeline

Milestone events only (buffer preambles, positioning FEEDL writes,
lamp transitions) — not every register write. Preamble classification
beyond wIndex is a heuristic (see `tools/phase_segment.py` docstring),
not a certainty. `rel_s` is seconds since this capture's first packet.

| idx | rel_s | kind | detail |
|---|---|---|---|
